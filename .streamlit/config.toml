# Streamlit settings for this repository. `streamlit run` reads this file from the
# folder you run it in, so run your apps from the repository root.
[server]
port = 28502
headless = true

[browser]
gatherUsageStats = false
